Your error is caused by duplicate declarations and exports in your use-auth.tsx and financial-dashboard.tsx files. Here’s how to fix it:

Steps to Fix the Errors:
1️⃣ Fix Duplicate AuthContext and AuthProvider in use-auth.tsx
You have two declarations of AuthContext, AuthProvider, and useAuth(), which are causing conflicts.
Open client/src/hooks/use-auth.tsx and remove one of each duplicate.
✅ Example Corrected Code (Ensure Only One Declaration Exists):

tsx
Copy
Edit
import { createContext, useContext, ReactNode } from "react";

export type AuthContextType = {
  user: any;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
};

export const AuthContext = createContext<AuthContextType | null>(null);

export function AuthProvider({ children }: { children: ReactNode }) {
  // Your auth logic here
  return (
    <AuthContext.Provider value={{ user: null, login: async () => {}, logout: () => {} }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  return useContext(AuthContext);
}
Ensure there is only one AuthContext, one AuthProvider, and one useAuth() export.
Remove extra exports from later in the file.
2️⃣ Fix Duplicate FinancialDashboardPage in financial-dashboard.tsx
You have two default exports of FinancialDashboardPage in client/src/pages/financial-dashboard.tsx.
Ensure there is only one export default FinancialDashboardPage.
✅ Example Corrected Code:

tsx
Copy
Edit
function FinancialDashboardPage() {
  return (
    <div>
      <h1>Financial Dashboard</h1>
    </div>
  );
}

export default FinancialDashboardPage;
Check for duplicate declarations in the same file and remove them.
3️⃣ Restart the Development Server
After making these changes, restart your development server:

bash
Copy
Edit
npm run dev
or

bash
Copy
Edit
yarn dev
or if using Vite:

bash
Copy
Edit
vite
Final Summary:
Fix duplicate AuthContext, AuthProvider, and useAuth() in use-auth.tsx.
Ensure FinancialDashboardPage is only exported once in financial-dashboard.tsx.
Restart the server after fixing the code.
This should resolve the issue. Let me know if you need further help! 🚀